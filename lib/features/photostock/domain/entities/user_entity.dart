/// [UserEntity] class is an class which is used to define the structure of the user entity.
class UserEntity {
  /// Username
  final String username;

  UserEntity({
    required this.username,
  });
}
